<?php require_once 'layout/header.phtml' ?>

<section class="show_series container my-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold text-dark">Series</h2>
        <?php if ($user): ?>
            <a href="addSerie" class="btn btn-primary btn-sm">+ Nueva Serie</a>
        <?php endif; ?>
    </div>

    <div class="row g-4">
        <?php foreach($series as $s): ?>
            <div class="col-sm-6 col-md-4 col-lg-3">
                <div class="card shadow-sm border-0 h-100">
                    <a href="list_series/<?= $s->id_serie ?>" class="text-decoration-none text-dark">
                        <img src="<?= $s->img ?>" class="card-img-top" alt="<?= htmlspecialchars($s->titulo) ?>" style="height: 250px; object-fit: cover;">
                        <div class="card-body text-center">
                            <h5 class="card-title fw-semibold mb-2"><?= htmlspecialchars($s->titulo) ?></h5>
                        </div>
                    </a>
                    <?php if ($user): ?>
                        <div class="card-footer bg-transparent border-0 text-center">
                            <a href="editSerie/<?=$s->id_serie?>" class="btn btn-warning btn-sm me-2">Editar</a>
                            <a href="deleteSerie/<?=$s->id_serie?>" class="btn btn-danger btn-sm">Eliminar</a>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        <?php endforeach; ?>
    </div>
</section>

<?php require_once 'layout/footer.phtml' ?>